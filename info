•Lenguaje y Framework
Java 17 / 25 .

Spring Boot 3.5.9 → 

•Base de Datos y ORM
SQL Server 16.0 → 

Hibernate 6.6.39. → ORM.

Spring Data JPA → Abstracción sobre Hibernate que te permite usar repositorios (findById, findAll) 

•Seguridad y Autenticación
Spring Security → Framework de seguridad para proteger endpoints.

JWT (JSON Web Token) → Tokens firmados que se envían en el header Authorization para identificar al usuario.

JwtAuthFilter → Filtro personalizado que intercepta cada request, valida el token y extrae los claims (ejemplo: role=ADMIN_EMPRESA, tenantDb=10758336773).

BCrypt → Algoritmo de encriptación usado para guardar contraseñas de forma segura.

•Arquitectura Multi-Tenant
TenantRoutingDataSource → Clase que decide a qué base de datos conectarse según el tenantDb del token JWT.

HikariCP → Pool de conexiones que gestiona eficientemente las conexiones a SQL Server.

•Entidades y Relaciones
....
•Relaciones 
....
📑 DTOs y Servicios
DTO (Data Transfer Object) → Objetos que controlan qué datos se devuelven en las respuestas, evitando ciclos infinitos y respuestas demasiado grandes.

Services (ProductService, InvoiceService, ClientService) → 

Controllers (ProductController, InvoiceController, ClientController, ) 

Maven y Build
Maven → gestión de dependencias.

spring-boot-maven-plugin : ejecutar la aplicación con mvn spring-boot:run.

.gitignore → Archivo para excluir target/, .idea/, .vscode/, etc. del repositorio.

Endpoints implementados
Autenticación: /auth/login

Productos: POST /product/createproduct, GET /product/listproduct, GET /productos/listproduct/{id}.

Clientes: POST /client/createclient, GET /client/listclient , GET /client/client/{id}

Facturas: POST /invoices/createinvoices ,GET /invoices/listinvoices/, GET /invoices/listinvoices/{id} → 
